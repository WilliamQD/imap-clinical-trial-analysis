# Validation Notes

R/Rscript is not available in the local Codex runtime used to prepare this public repo, so executable R tests were not run here.

Validation performed before publication:

- no clinical data files included,
- no row-level CSV/XLSX/RDS artifacts included,
- public documentation limited to aggregate results,
- R scripts rewritten as parameterized templates without participant IDs or hard-coded correction rows.
